<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.ypst.primeyz.newsapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
            android:name=".root.NewsApp"
            android:allowBackup="true"
            android:icon="@drawable/news_app_logo"
            android:label="@string/app_name"
            android:roundIcon="@drawable/news_app_logo"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activities.SearchActivity">
        </activity>
        <activity android:name=".activities.SourceListActivity">
        </activity>
        <activity android:name=".activities.HeadLinesBySourceActivity">
        </activity>

        <meta-data
                android:name="io.fabric.ApiKey"
                android:value="b2e156443d4ba9677318dd7d5df65291546d1880" />

    </application>

</manifest>